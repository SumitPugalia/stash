{erl_opts, [debug_info]}.
{deps, []}.

{ct_opts, [
  {sys_config, ["config/test.config"]},
  {ct_hooks, [cth_surefire]}
]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.

{shell, [
  {config, ["config/test.config"]},
  {apps, [stash]}
]}.
